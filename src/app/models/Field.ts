import {TemplateOptions} from './TemplateOptions';

export class Field {
  id ?: string;
  key ?: any;
  templateOptions ?: TemplateOptions;
  type ?: string;
  fieldGroup?: any[];
  fieldGroupId?: any[];
}
